# HTTPS proxy for SkyBridge Kneeboard — required for browser GPS
server.modules += ("mod_proxy")

$SERVER["socket"] == ":8443" {
    ssl.engine = "enable"
    ssl.pemfile = "/etc/lighttpd/certs/server.pem"

    proxy.server = ( "" => (( "host" => "127.0.0.1", "port" => 8083 )) )
}
